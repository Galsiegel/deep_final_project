name: deep_final_project
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - pip
  - pytorch>=2.0.0
  - torchvision
  - torchaudio
  - cudatoolkit=11.8  # Adjust based on your CUDA version, or remove if CPU-only
  - pip:
    - transformers>=4.30.0
    - torch>=2.0.0
    - requests>=2.31.0
    - python-dotenv>=1.0.0
    - massive
    - numpy>=1.24.0
    - pandas>=2.0.0
    - scikit-learn>=1.3.0

